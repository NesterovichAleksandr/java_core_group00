Три строки будет храниться в String Pool:

     1.   String s1 = "Cat1";
     2.   String s2 = "Cat";
        String s3 = new String ("Cat"); // эта не заносится в стринг пул
     3.   String s4 = s3.intern(); // тут проверяется есть ли в пуле строк Cat. есть. берётся ссылка на Cat и эта ссылка будет равна ссылке которая хранится в s2

     что делает и как работает метод intern()?

     Ответ:     Проверяет есть ли такая строка в пуле и добавляет, следовательно данная строка уже есть в пуле
     следовательно, она как бы заносится (на самом деле если такая строка уже есть в пуле, то она не заносится, а берётся ссылка на существующую строку и сохраняется в переменную) но на место номера 2))) как то так

